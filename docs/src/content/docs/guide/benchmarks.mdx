---
title: Benchmarks
description: Performance benchmarks and methodology
---

import { Tabs, TabItem, Card, CardGrid, Aside } from '@astrojs/starlight/components';

Run your own benchmarks to see real performance on your hardware.

## Quick Results

<CardGrid>
  <Card title="Push Throughput" icon="star">
    **50,000 - 150,000 ops/sec**

    Depends on payload size and batch operations
  </Card>
  <Card title="Pull Latency (p99)" icon="stopwatch">
    **&lt; 1ms**

    Sub-millisecond for most workloads
  </Card>
  <Card title="Memory Footprint" icon="laptop">
    **~50MB base**

    Plus ~100 bytes per queued job
  </Card>
  <Card title="Cold Start" icon="rocket">
    **&lt; 100ms**

    Ready to process jobs almost instantly
  </Card>
</CardGrid>

---

## Run Benchmarks Yourself

<Aside type="tip">
The only honest benchmark is one you run on your own hardware.
</Aside>

```bash
git clone https://github.com/egeominotti/bunqueue.git
cd bunqueue
bun install

# Run all benchmarks
bun run bench

# Individual benchmarks
bun run bench:throughput
bun run bench:latency
bun run bench:internal
```

---

## Throughput Benchmark

Measures jobs pushed and processed per second.

<div style={{ textAlign: 'center', margin: '2rem 0' }}>
  <img
    src="https://quickchart.io/chart?c={type:'bar',data:{labels:['Single','Bulk(10)','Bulk(100)','Bulk(1k)'],datasets:[{label:'ops/sec',data:[55000,95000,120000,145000],backgroundColor:['rgba(236,72,153,0.8)','rgba(168,85,247,0.8)','rgba(59,130,246,0.8)','rgba(34,197,94,0.8)'],borderRadius:6}]},options:{plugins:{title:{display:true,text:'Throughput Performance',font:{size:16,weight:'bold'}},legend:{display:false}},scales:{y:{beginAtZero:true,title:{display:true,text:'Operations/sec'},grid:{color:'rgba(0,0,0,0.1)'}},x:{grid:{display:false}}}}}&width=500&height=300&backgroundColor=white"
    alt="Throughput benchmark chart"
    style={{ maxWidth: '500px', width: '100%', borderRadius: '12px', boxShadow: '0 4px 6px -1px rgba(0,0,0,0.1)' }}
  />
</div>

| Operation | Ops/sec | vs Single |
|-----------|---------|-----------|
| Single Push | **~55,000** | 1x |
| Bulk (10) | **~95,000** | 1.7x |
| Bulk (100) | **~120,000** | 2.2x |
| Bulk (1000) | **~145,000** | 2.6x |

<Aside>
Batch operations significantly improve throughput. Batch size of 100-1000 provides best performance.
</Aside>

---

## Latency Benchmark

Measures time to complete operations in milliseconds.

<div style={{ textAlign: 'center', margin: '2rem 0' }}>
  <img
    src="https://quickchart.io/chart?c={type:'line',data:{labels:['p50','p95','p99','p99.9'],datasets:[{label:'Push',data:[0.05,0.12,0.25,0.60],borderColor:'rgb(236,72,153)',backgroundColor:'rgba(236,72,153,0.1)',fill:true,tension:0.3},{label:'Process',data:[0.02,0.08,0.15,0.35],borderColor:'rgb(59,130,246)',backgroundColor:'rgba(59,130,246,0.1)',fill:true,tension:0.3}]},options:{plugins:{title:{display:true,text:'Latency Distribution (ms)',font:{size:16,weight:'bold'}}},scales:{y:{beginAtZero:true,title:{display:true,text:'Milliseconds'},grid:{color:'rgba(0,0,0,0.1)'}},x:{title:{display:true,text:'Percentile'},grid:{display:false}}}}}&width=500&height=300&backgroundColor=white"
    alt="Latency benchmark chart"
    style={{ maxWidth: '500px', width: '100%', borderRadius: '12px', boxShadow: '0 4px 6px -1px rgba(0,0,0,0.1)' }}
  />
</div>

| Percentile | Push | Process | Round-trip |
|------------|------|---------|------------|
| **p50** | 0.05ms | 0.02ms | 0.08ms |
| **p95** | 0.12ms | 0.08ms | 0.20ms |
| **p99** | 0.25ms | 0.15ms | 0.40ms |
| **p99.9** | 0.60ms | 0.35ms | 0.95ms |

<Aside type="tip">
Sub-millisecond latency even at p99.9 percentile.
</Aside>

---

## Memory Usage

Memory footprint under different workloads.

<div style={{ textAlign: 'center', margin: '2rem 0' }}>
  <img
    src="https://quickchart.io/chart?c={type:'bar',data:{labels:['Idle','10K jobs','100K jobs','1M jobs'],datasets:[{label:'RSS Memory (MB)',data:[50,65,150,400],backgroundColor:'rgba(139,92,246,0.8)',borderRadius:6}]},options:{plugins:{title:{display:true,text:'Memory Footprint',font:{size:16,weight:'bold'}},legend:{display:false}},scales:{y:{beginAtZero:true,title:{display:true,text:'Memory (MB)'},grid:{color:'rgba(0,0,0,0.1)'}},x:{grid:{display:false}}}}}&width=500&height=300&backgroundColor=white"
    alt="Memory usage chart"
    style={{ maxWidth: '500px', width: '100%', borderRadius: '12px', boxShadow: '0 4px 6px -1px rgba(0,0,0,0.1)' }}
  />
</div>

| Queue Depth | RSS Memory | Heap Used | Per Job |
|-------------|------------|-----------|---------|
| Idle | **~50 MB** | ~25 MB | - |
| 10,000 jobs | **~65 MB** | ~40 MB | ~1.5 KB |
| 100,000 jobs | **~150 MB** | ~100 MB | ~1.0 KB |
| 1,000,000 jobs | **~400 MB** | ~300 MB | ~350 B |

<Aside>
Memory usage scales linearly. Efficient ~100 bytes per job at scale.
</Aside>

---

## Internal Optimizations

bunqueue uses optimized data structures internally.

```bash
bun run bench:internal
```

<Tabs>
  <TabItem label="Skip List">
    Used for priority queue ordering. O(log n) insert vs O(n) array splice.

    | Elements | Array Splice | Skip List | Speedup |
    |----------|-------------|-----------|---------|
    | 1,000 | 12ms | 2ms | **6x** |
    | 5,000 | 180ms | 8ms | **22x** |
    | 10,000 | 650ms | 15ms | **43x** |
  </TabItem>

  <TabItem label="LRU Cache">
    Doubly-linked list vs Map delete+set for move-to-front.

    | Cache Size | Naive | Optimized | Speedup |
    |------------|-------|-----------|---------|
    | 1,000 | 45ms | 12ms | **3.7x** |
    | 5,000 | 52ms | 14ms | **3.7x** |
    | 10,000 | 58ms | 15ms | **3.9x** |
  </TabItem>

  <TabItem label="MinHeap">
    Used for delayed job scheduling. O(log n) operations.

    | Elements | Sorted Array | MinHeap | Speedup |
    |----------|-------------|---------|---------|
    | 1,000 | 8ms | 1ms | **8x** |
    | 5,000 | 95ms | 5ms | **19x** |
    | 10,000 | 320ms | 10ms | **32x** |
  </TabItem>
</Tabs>

---

## Why bunqueue is Fast

<CardGrid>
  <Card title="Native SQLite" icon="seti:db">
    Direct FFI bindings via `bun:sqlite`. WAL mode for concurrent reads/writes. Memory-mapped I/O.
  </Card>
  <Card title="Efficient Data Structures" icon="list-format">
    Skip lists for O(log n) priority ordering. MinHeap for delayed jobs. LRU cache with O(1) access.
  </Card>
  <Card title="Batch Optimization" icon="rocket">
    Single transaction for bulk operations. Prepared statements reused. 32 shards minimize lock contention.
  </Card>
  <Card title="Zero Overhead" icon="approve-check">
    JSON stored directly in SQLite. No protocol encoding/decoding. MessagePack for binary data.
  </Card>
</CardGrid>

---

## Benchmark Environment

For reproducible results, note your environment:

```bash
# System info
uname -a
bun --version

# CPU info (macOS)
sysctl -n machdep.cpu.brand_string

# CPU info (Linux)
cat /proc/cpuinfo | grep "model name" | head -1
```

**Reference hardware** (used for example numbers):
- Mac Studio, Apple M1 Max, 32GB RAM
- macOS 26.2 (Tahoe)
- Bun 1.3.8
- SSD storage

---

## Reproduce These Results

All benchmark code is in the repository:

```
bench/
├── throughput.ts   # Push/pull ops per second
├── latency.ts      # Operation latency percentiles
benchmarks/
└── index.ts        # Internal data structure benchmarks
```

Submit your results via [GitHub Discussions](https://github.com/egeominotti/bunqueue/discussions) to help others compare hardware.
